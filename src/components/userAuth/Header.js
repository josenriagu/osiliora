import React from 'react';
import { HeaderStyle } from '../../styled/userAuthStyles';

export default function Header() {
   return (
      <HeaderStyle>
         <h2>I'll Serve Soup</h2>
      </HeaderStyle>
   );
}